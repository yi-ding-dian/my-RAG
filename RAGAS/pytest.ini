[pytest]
# 测试目录（收集范围为 tests/，避免递归收集到 frontend/dist 等构建产物）
testpaths = tests
# 排除第三方与构建产物目录（否则本地会收集到 node_modules/dist 下的 test_*.py）
norecursedirs = node_modules .venv data dist .git frontend docker docs backend prompts reports
