[pytest]
# 排除第三方参考材料与构建产物目录（否则本地会收集到 参考/ 下的 test_*.py）
norecursedirs = 参考 node_modules .venv data dist .git frontend docker docs mcp-server scripts deploy

markers =
    milvus: 需要真实 Milvus 服务的集成测试（连不上自动 skip，可用 -m "not milvus" 排除）
